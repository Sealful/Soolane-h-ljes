# Enemy System Implementation Plan

## Summary
Implement a modular enemy system for the pygame top-down shooter with the following features:
- Spawn function for enemies
- Pathfinding toward the player
- Multiple enemy types (extensible framework)
- Bullet-based damage/kill system
- Clean code organization and comments

## Implementation Details

### 1. Enemy Base Class (`Enemy`)
Generic enemy with health, speed, damage, collision, and visualization.

**Attributes:**
- `pos` (Vector2): World position
- `radius` (int): Collision radius
- `speed` (float): Movement speed
- `health` (int): Current health
- `max_health` (int): Maximum health
- `damage` (int): Damage on player collision
- `color` (tuple): Visual color (RGB)
- `points` (int): Score value

**Methods:**
- `update(dt, player_pos)`: Move toward player each frame
- `take_damage(amount)`: Reduce health, return True if died
- `is_alive()`: Check if health > 0
- `draw(screen, camera_offset)`: Render the enemy

### 2. Enemy Type Registry
Factory pattern using a dictionary to create enemies by type name.

**Types (initial):**
- `basic` - Standard enemy: medium speed, low health, direct charge
- `fast` - High speed, very low health, quick attacks
- `tank` - Low speed, high health, high damage

Each type is a function returning an Enemy instance with specific stats.

### 3. Randomized Spawn System
Enemies spawn at random positions along the map border with no predetermined points.

**Spawn Zone Generation:**
- Pick a random angle (0-360 degrees) from map center
- Find the intersection point on the map edge at that angle
- Add random jitter along the edge segment for variation
- Spawn enemies just outside the map boundary

**Wave System:**
- Spawn waves of 1-4 enemies at once instead of single spawns
- Each enemy in a wave gets a random type from the registry
- Small spread radius so wave enemies spawn near each other
- Fixed spawn interval (can be made dynamic in future)

**Configuration:**
- `SPAWN_INTERVAL`: Time between waves (e.g., 2.0s)
- `MAX_ENEMIES`: Concurrent enemy cap (e.g., 20)
- `WAVE_SIZE_MIN` / `WAVE_SIZE_MAX`: 1 to 4 enemies per wave
- `SPAWN_JITTER`: Random offset along edge (e.g., 80 pixels)

**Future Hooks (not implemented now but designed for):**
- Dynamic difficulty: spawn rate could scale with score
- Directional preference: could bias spawns away from player movement
- Enemy type weighting: harder types could unlock over time

**Spawn Algorithm:**
```
1. Generate random angle (0-360)
2. Calculate intersection point on map edge at that angle
3. Add random jitter along the edge
4. Offset slightly outside map border
5. Spawn 1-N enemies with small spread around point
6. Each enemy gets random type from registry
```
1. Generate random angle (0-360)
2. Calculate intersection point on map edge at that angle
3. Add random jitter along the edge
4. Offset slightly outside map border
5. Spawn 1-N enemies with small spread around point
6. Each enemy gets random type from registry
```
1. Generate random angle (0-360)
2. Calculate intersection point on map edge at that angle
3. Add random jitter along the edge
4. Offset slightly outside map border
5. Spawn 1-N enemies with small spread around point
6. Each enemy gets random type from registry
```

### 4. Pathfinding
Simple direct pursuit (seek behavior) toward player.

**Behavior:**
- Calculate direction vector: `player_pos - enemy_pos`
- Normalize and multiply by speed * dt
- Move toward player
- For future: add obstacles/waypoints or A*

### 5. Collision & Damage System
**Bullet-Enemy Collision:**
- Check if projectile position is within enemy radius
- Remove projectile on hit
- Apply damage, spawn death effects (score)
- Remove dead enemies

**Player-Enemy Collision:**
- Check if player is within enemy radius + player_radius
- Deal damage to player (future feature)
- Knockback (future)

### 6. Modular File Structure
Split into separate modules for maintainability:

```
game.py                  - Main game loop, setup, rendering
enemy.py                 - Enemy base class and type definitions
spawn_manager.py         - Enemy spawning logic
combat_system.py         - Collision detection, damage, scoring
```

### 7. Code Quality
- Full docstrings for all classes and functions
- Type hints where appropriate
- Consistent naming (snake_case)
- Section headers and comments
- Constants for all tunable values

### 8. Rendering
- Color-coded enemies by type
- Health bar for tanks (future)
- Death particle effects (simple)
- Smooth animation (interpolation)

## Step-by-Step Tasks

1. Create `enemy.py` - Enemy base class with type registry factory
2. Create `spawn_manager.py` - Randomized wave spawn with angular border selection
3. Create `combat_system.py` - Bullet-enemy collision, damage, enemy removal
4. Modify `game.py` - Integrate enemy module imports, enemy list, spawn manager init
5. Update game loop - Call spawn update, enemy update, combat check each frame
6. Add enemy rendering - Draw enemies in world space with camera offset
7. Tune balance values - Adjust spawn interval, speeds, health, wave sizes
8. Verify enemy death and removal works cleanly

## Future Extensibility
- Add new `register_enemy_type("name", factory_func)` calls
- Subclass Enemy for custom AI behaviors
- Add enemy projectiles
- Add wave-based spawns
- Add boss enemies
- Add pathfinding for obstacles